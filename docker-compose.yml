services:
 tavern-card-helper:
 build:.
 ports:
 - "3001:3001"
 environment:
 - NODE_ENV=production
 - PORT=3001
 - HOST=0.0.0.0
# Nếu bạn muốn hạn chế quyền truy cập vào tên miền, hãy bỏ ghi chú và điền vào:
 # - CORS_ORIGINS=https://your-domain.com
 restart: unless-stopped
